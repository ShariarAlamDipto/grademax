# Python dependencies for GradeMax page-based ingestion system
# Core PDF processing
PyMuPDF==1.23.8
pikepdf==8.10.1
PyPDF2==3.0.1  # For PDF merging

# Google Gemini API for LLM classification
google-generativeai==0.3.2

# OCR (optional, for scanned PDFs)
pytesseract==0.3.10
# Note: Tesseract binary must be installed separately

# YAML parsing for topic catalogue
PyYAML==6.0.1

# Database
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.0
requests==2.31.0
tqdm==4.66.1
